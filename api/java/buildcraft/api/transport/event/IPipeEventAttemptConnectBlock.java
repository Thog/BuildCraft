package buildcraft.api.transport.event;

/** Fired whenever a block (That is NOT a pipe) attempts to connect to this pipe. */
public interface IPipeEventAttemptConnectBlock extends IPipeEventAttemptConnect {}
